import styled from 'styled-components';

const AppWrapper = styled.div`
width: 100%;
height:100%;
background-color: #EDECE6;
`;

const AppMainWrapper = styled.section`
  width: 100%;
  min-height: 90vh;
`;

export default { AppMainWrapper,AppWrapper };
